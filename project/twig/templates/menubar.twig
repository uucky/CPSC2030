<div class="menubar">
    <div class="hamburger" onclick="ham()">☰ Menu</div>
          <div class="menu btn16" data-menu="16"></div>
        <div class="icon"></div>
    <div class="menus">
        <ul>
            {% for link in menu %}
            <li>
                <a href="{{link["menuLink"]}}">
                    <span>{{link["menuItem"]}}</span>
                </a>
            </li>
            {% endfor %}
        </ul>
    </div>
</div>