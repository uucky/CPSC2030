<!-- Pokemon table: Create a twig for your Pokémon table
Should accept the Pokedex table as a parameter
You will need the fetch_all method on the sql result object -->

<section class="grid-container">
        {% for item in items %}
        <div class="box">
            {# <td><a href="card.php?pokemon_id={{ poke[ "id" ] }}">{{poke["id"]}}</a></td> #}
            <div class="issueId">ISSUE {{item["issueId"]}}</div>
            <div class="itemName">{{item["name"]}}</div>
            {# <td>{{item["issueId"]}} #}
            <img src="res/ships/{{item["issueId"]}}.jpg" alt="{{item["issueId"]}}">
            <div class="price">${{item["price"]}} Credits</div>
            {# {% for item in poke %}
                <td>{{ item }}</td>
            {% endfor %} #}
        </div>
        {% endfor %}
</section>